import deleteMessage from './deleteMessage.middleware';
import editMessage from './editMessage.middleware';
import getMessages from './getMessages.middleware';
import newMessage from './newMessage.middleware';

export default {
    deleteMessage,
    editMessage,
    getMessages,
    newMessage,
};
